-if flash.keys.any?
  .container
    -flash.each do |type, msg|
      .alert{class: "alert-#{type == 'success' ? 'success' : 'danger'}"}
        %a.close{'data-dismiss' => 'alert'} X
        %div{id: "#{type}"}= msg
